sudo: required
dist: bionic
language: cpp

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install python3

install:
  - sudo apt-get -y install libarmadillo-dev

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
